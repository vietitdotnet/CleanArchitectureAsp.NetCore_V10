﻿@{
        ViewData["title"] = "Promotions";
        var statusMessage = TempData["StatusMessage"] as string;
}

@* 
    Layout dự phòng nếu không được chỉ định trong các view con
*@

@section Scripts {
    @RenderSection("Scripts", required: false)
}